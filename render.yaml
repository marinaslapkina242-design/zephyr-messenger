services:
  - type: web
    name: zephyr-messenger
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
    healthCheckPath: /
    plan: free